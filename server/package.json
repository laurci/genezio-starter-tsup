{
    "name": "server",
    "devDependencies": {
        "tsup": "~6.6.0",
        "typescript": "^5.0.2"
    },
    "scripts": {
        "build": "tsup",
        "deploy": "yarn build && genezio deploy",
        "local": "yarn build && genezio local"
    }
}
